# 0021_function_kwargs.py (함수 가변 키워드 매개변수)
# 목적: 별표 두 개(**)를 활용하여 '이름표'가 붙은 여러 데이터를 한 번에 입력받기
# -------------------------------------------------------
# Scenario:
# - 미디어 아트 전시에 사용될 다양한 디스플레이 장비(프로젝터, LED 벽 등)를 세팅함.
# - 장비마다 필요한 설정값(해상도, 밝기, 프레임 등)의 종류와 개수가 모두 다름.
# - 함수는 설정값의 '이름'과 '수치'를 쌍으로 묶어 하나의 시스템(사전 형식)으로 받아들임.
# - 입력된 세팅값 전체를 확인하여 장비가 올바르게 구성되었는지 모니터링함.
# -------------------------------------------------------

# 1. 스마트 세팅 엔진 정의 (Smart Configuration Engine)
# 💡 **settings: 이름표(Key)와 데이터(Value)가 쌍으로 묶인 사전(Dictionary) 형태로 모두 쓸어 담습니다.
def configure_display(**settings):
    print("디스플레이 세팅 정보가 업데이트되었습니다:")
    
    # settings 주머니에 어떤 이름표와 데이터들이 담겼는지 통째로 출력해 봅니다.
    print(settings)
    print("-" * 30) # 시각적 구분을 위한 점선 출력

# 2. 시스템 가동 및 커스텀 데이터 입력 (System Execution)
# [장비 A] 메인 프로젝터: 2개의 설정값 전달
configure_display(resolution="4K", framerate=60)

# [장비 B] 보조 LED 벽: 3개의 전혀 다른 설정값 전달
configure_display(brightness=100, color="RGB", color_mode="Cinema")