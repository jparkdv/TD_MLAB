# 0017_function_basics.py (함수 기본 구조 및 매개변수 활용)
# 목적: 함수의 매개변수 기본값 설정을 통한 시스템 제어의 유연성 확보
# -------------------------------------------------------
# Scenario:
# - 전시장의 다양한 구역별 조명을 제어하되, 반복되는 설정값 입력을 최소화함.
# - 기본 밝기를 80으로 지정하여, 별도의 명령이 없어도 최적의 전시 환경을 유지함.
# - 특정 구역(하이라이트, 절전 등)에 한해 가변적인 조도 조절 기능을 제공함.
#
# Requirements:
# - 필수 매개변수(zone)와 선택적 매개변수(level=80)를 포함한 함수 설계
# - if-else 조건문을 활용한 기본/사용자 지정 상태 구분 출력
# - 기본값 호출 및 사용자 지정 값 호출을 통한 로직 검증
# -------------------------------------------------------

# 1. 스마트 조명 제어 엔진 (Smart Lighting Engine)
# 💡 level 매개변수에 기본값(=80)을 부여하여 호출 시 생략 가능하도록 설계
def set_lighting(zone, level=80):
    if level == 80:
        print(f"[{zone}] 조명 시스템: 기본 밝기 {level}%로 설정되었습니다.")
    else:
        print(f"[{zone}] 조명 시스템: 사용자 지정 밝기 {level}%로 설정되었습니다.")

# 2. 구역별 시스템 가동 (System Execution)
# [CASE A] Lobby: 밝기 값을 생략 -> 자동으로 80% 적용
set_lighting("Lobby")

# [CASE B] Gallery_A: 특정 전시를 위해 100%로 강제 지정
set_lighting("Gallery_A", 100)

# [CASE C] Rest_Area: 휴식을 위해 30%로 강제 지정
set_lighting("Rest_Area", 30)